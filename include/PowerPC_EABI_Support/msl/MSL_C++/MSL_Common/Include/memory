#ifndef _MSL_MEMORY
#define _MSL_MEMORY

namespace std {

template <typename T>
struct allocator {
    // FIXME: This needs to be different for other types, if we end up needing them
    allocator() {}

    // TODO
};

}

#endif
